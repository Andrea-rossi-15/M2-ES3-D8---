using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.Rendering.Universal;

public class eserFacoltativo3 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Tabellina 1
        Debug.Log("tabellina 1");
        int n = 10;
        int var = 1;
        for (int contatore = 1; contatore <= n; contatore = contatore + var)
        {
            Debug.Log(contatore);
        }
        //tabellina 2
        Debug.Log("tabellina 2");
        n = 20;
        var++;
        for (int contatore = 2; contatore <= n; contatore = contatore + var)
        {
            Debug.Log(contatore);
        }
        //Tabellina 3
        Debug.Log("tabellina 3");
        n = 30;
        var++;
        for (int contatore = 3; contatore <= n; contatore = contatore + var)
        {
            Debug.Log(contatore);
        }
        //Tabellina 4
        Debug.Log("tabellina 4");
        n = 40;
        var++;
        for (int contatore = 4; contatore <= n; contatore = contatore + var)
        {
            Debug.Log(contatore);
        }
        //Tabellina 5
        Debug.Log("tabellina 5");
        n = 50;
        var++;
        for (int contatore = 5; contatore <= n; contatore = contatore + var)
        {
            Debug.Log(contatore);
        }
        //Tabellina 6
        Debug.Log("tabellina 6");
        n = 60;
        var++;
        for (int contatore = 6; contatore <= n; contatore = contatore + var)
        {
            Debug.Log(contatore);
        }
        Debug.Log("tabellina 7");
        n = 70;
        var++;
        for (int contatore = 7; contatore <= n; contatore = contatore + var)
        {
            Debug.Log(contatore);
        }
        //Tabellina 8
        Debug.Log("tabellina 8");
        n = 80;
        var++;
        for (int contatore = 8; contatore <= n; contatore = contatore + var)
        {
            Debug.Log(contatore);
        }
        //Tabellina 9
        Debug.Log("tabellina 9");
        n = 90;
        var++;
        for (int contatore = 9; contatore <= n; contatore = contatore + var)
        {
            Debug.Log(contatore);
        }
        //Tabellina 10
        Debug.Log("tabellina 10");
        n = 100;
        var++;
        for (int contatore = 10; contatore <= n; contatore = contatore + var)
        {
            Debug.Log(contatore);
        }

    }

}








